package at.technikum_wien.polzert.news.data

import kotlinx.datetime.Clock

/*
fun createDummyNews(count : Int = 30) : List<NewsItem> {
    return (1..count).map {
        NewsItem(
            title = "Title $it",
            description = "Description $it",
            author = "Author $it",
            publicationDate = Clock.System.now(),
            keywords = setOf("Keyword_${it}_0", "Keyword_${it}_1", "Keyword_${it}_2")
        )
    }
}*/
